# my-portfolio-server

Node.js and Express API for the portfolio contact form.

## Run locally

```bash
npm install
npm start
```

Set `MONGODB_URI` and other server settings in `.env` before starting the API.